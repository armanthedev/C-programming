#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    /*
    int ar[5];
        ar[0] = 100;
        ar[1] = 200;
        ar[2] = 300;
        ar[3] = 400;
        printf("%d\n%d\n%d\n%d", ar[0], ar[1], ar[2], ar[3]);


    int arr[5];
    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &arr[i]);
    }
    for (int i = 0; i < 5; i++)
    {
        printf("ar[%d]\n", arr[i]);
    }


    // array initialization

    int arr[5] = {100, 400, 700, 1000, 3000};
    for (int i = 0; i < 5; i++)
    {
        printf("%d\n", arr[i]);
    }
    */
    return 0;
}
